# No redirects needed.
# Cloudflare Pages automatically:
# 1. Serves static files (e.g. /p/demo/ -> /p/demo/index.html)
# 2. Serves 404.html for unmatched paths (e.g. /p/lucas-test -> 404.html)
# The custom not-found.tsx detects /p/* paths and loads from Supabase dynamically.
